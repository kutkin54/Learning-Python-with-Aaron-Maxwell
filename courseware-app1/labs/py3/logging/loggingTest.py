import logging
logging.basicConfig(level=logging.DEBUG)
logging.debug('This is a debug')
logging.info('This is informative')
logging.warning('Look out!')
logging.error('This is an error!')
logging.critical('ups abort!')
